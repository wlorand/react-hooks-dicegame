import React from 'react';
import ReactDOM from 'react-dom';

// Root Component
import DiceGame from './DiceGame';

// styles
import './index.css';

ReactDOM.render(<DiceGame />, document.getElementById('mount-point'));
